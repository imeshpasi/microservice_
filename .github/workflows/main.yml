name: Build and Push Docker Image to Azure Container Registry

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: microservicecon.azurecr.io
          username: microservicecon   # Set your ACR username in GitHub Secrets
          password: 3qZV8ZC53McCVQhwWMiVNT6sNFXymDYNVyV52L+lZ0+ACRAQ2gIl

